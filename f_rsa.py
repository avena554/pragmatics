import math
from math import exp


def ls0(types, messages, lexicon, alpha):
    for(m in messages):
        j = 0
        if(t in lexicon[m]):
            j = exp(alpha*)
    

def nextL(types, lexicon, seq):
    
